apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubefay.fullname" . }}
  labels:
    {{- include "kubefay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubefay.selectorLabels" . | nindent 4 }}
