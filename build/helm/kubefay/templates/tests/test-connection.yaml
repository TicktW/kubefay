apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubefay.fullname" . }}-test-connection"
  labels:
    {{- include "kubefay.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubefay.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
